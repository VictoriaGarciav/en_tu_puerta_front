import 'package:flutter/material.dart';

// Widget para la pantalla de ajustes del proveedor
class WidgetProviderSettings extends StatelessWidget {

  const WidgetProviderSettings({super.key});

  @override
  Widget build(BuildContext context) {
    // Retorna un contenedor gris con texto de prueba para ajustes

    return Container(
      color: Colors.grey,
      child: Text('Index 3: Ajustes'),
      
    );
  }
}
