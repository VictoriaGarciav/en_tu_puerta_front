import 'package:flutter/material.dart';

// Widget para la pantalla de métricas del proveedor
class WidgetProviderMetrics extends StatelessWidget {

  const WidgetProviderMetrics({super.key});

  @override
  Widget build(BuildContext context) {
    // Retorna un contenedor gris con texto de prueba para métricas

    return Container(
      color: Colors.grey,
      child: Text('Index 2: Metrics'),
      
    );
  }
}
