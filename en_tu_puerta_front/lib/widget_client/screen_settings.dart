import 'package:flutter/material.dart';

// Widget para la pantalla de ajustes del cliente
class WidgetSettings extends StatelessWidget {

  const WidgetSettings({super.key});

  @override
  Widget build(BuildContext context) {
    // Retorna un contenedor gris con texto de prueba para ajustes

    return Container(
      color: Colors.grey,
      child: Text('Index 3: Ajustes'),
      
    );
  }
}
